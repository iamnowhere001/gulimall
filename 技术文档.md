# 谷粒商城（Gulimall）技术文档

> 本文档基于 `gulimall` 微服务项目源码整理，覆盖前台商城（登录、搜索、商品详情、购物车、下单、秒杀）与后台管理（商品、营销、库存、订单、会员）等核心模块的业务逻辑、数据流转、技术难点与解决方案。

> **阅读说明（严谨性）**：文中**端口、网关路由、枚举值、关键 SQL、注解开关（如 `seata.enabled`）、Feign 路径**等均已对照仓库源码核对；标注「*源码推断*」之处为基于命名/配置的合理推断而非逐行确认。第 14 章汇总了源码审视中发现的局限与瑕疵。

---

## 目录

1. [项目概述与技术架构](#1-项目概述与技术架构)
2. [整体架构与模块依赖关系](#2-整体架构与模块依赖关系)
3. [统一网关与基础设施模块](#3-统一网关与基础设施模块)
4. [认证与单点登录模块（auth-server）](#4-认证与单点登录模块auth-server)
5. [公共模块（common）与第三方服务（third-party）](#5-公共模块common与第三方服务third-party)
6. [商品模块（product）与检索模块（search）](#6-商品模块product与检索模块search)
7. [会员模块（member）与优惠券模块（coupon）](#7-会员模块member与优惠券模块coupon)
8. [购物车模块（cart）](#8-购物车模块cart)
9. [订单模块（order）与仓储模块（ware）](#9-订单模块order与仓储模块ware)
10. [秒杀模块（seckill）](#10-秒杀模块seckill)
11. [全局技术难点与解决方案汇总](#11-全局技术难点与解决方案汇总)
12. [中间件使用汇总](#12-中间件使用汇总)
13. [开发问题 QA（常见问题与解决方案）](#13-开发问题-qa常见问题与解决方案)
14. [已知局限与待完善（源码审视备注）](#14-已知局限与待完善源码审视备注)

---

## 1. 项目概述与技术架构

谷粒商城是一套前后端分离的电商系统，基于 **Spring Boot + Spring Cloud + Spring Cloud Alibaba + MyBatis-Plus** 实现，采用 **Docker** 容器化部署，Nacos 作为注册中心与配置中心。

### 1.1 技术选型

| 分类 | 技术 | 用途 |
|------|------|------|
| 微服务框架 | Spring Boot / Spring Cloud | 容器、MVC、微服务治理 |
| 服务治理 | Nacos | 服务注册发现 + 配置中心（8848） |
| ORM | MyBatis-Plus | 持久层、分页、代码生成 |
| 搜索引擎 | Elasticsearch 7.17 | 商品全文检索、聚合、高亮 |
| 消息队列 | RabbitMQ 3.8 | 异步解耦、最终一致性、延迟队列 |
| 缓存/分布式 | Redis + Redisson | 分布式 Session、分布式锁、信号量、缓存 |
| 分布式 Session | Spring Session | 子域 Session 共享（单点登录） |
| 对象存储 | 阿里云 OSS | 图片/文件上传 |
| 网关 | Spring Cloud Gateway | 统一路由、跨域 |
| 前端 | Vue + Element + Thymeleaf | 后台管理 / 前台页面 |

### 1.2 部署中间件（docker-compose）

`docker-compose.yml` 统一编排：`mysql(3306)`、`redis(6379)`、`elasticsearch(9200/9300)`、`kibana(5601)`、`rabbitmq(5672/15672)`、`nginx(80)`、`nacos(8848/9848/9849)`，数据均挂载到命名卷实现持久化。

---

## 2. 整体架构与模块依赖关系

### 2.1 模块划分

```
gulimall
├── gulimall-common         公共工具、统一返回、共享 VO、常量
├── gulimall-gateway        统一网关（路由 + 跨域）
├── gulimall-auth-server    认证中心（账号密码登录、社交登录、SSO、注册、短信）
├── gulimall-member         会员（注册/登录、等级、地址、积分、社交账号）
├── gulimall-coupon         营销（优惠券、满减、打折、会员价、秒杀场次）
├── gulimall-product        商品（SPU/SKU、分类、品牌、属性、上架）
├── gulimall-search         检索（ES 检索、商品上架写入）
├── gulimall-cart           购物车（Redis 实现，无独立 DB）
├── gulimall-ware           仓储（库存、锁定/解锁、采购）
├── gulimall-order          订单（下单、状态流转、支付、关单）
├── gulimall-seckill        秒杀（Redis 上架、信号量扣减、MQ 建单）
└── gulimall-third-party    第三方（OSS 上传、短信发送）
```

### 2.2 请求整体流转

```
浏览器 (gulimall.com / *.gulimall.com)
        │  Host 路由 / Path 路由 (lb:// 经 Nacos 负载均衡)
        ▼
  gulimall-gateway  ──(CORS / RewritePath)──►  各微服务
   (auth/product/cart/search/order/member/ware/seckill/coupon/third-party)
        │
        └─ 微服务间通过 Feign（带 Cookie 透传）+ MQ 协作
```

### 2.3 核心 Feign 远程调用网络

| 调用方 | 目标服务 | 主要用途 |
|--------|----------|----------|
| auth | member | 注册 / 账号密码登录 / 社交登录 |
| auth | third-party | 发送短信验证码 |
| member | coupon | 查询会员优惠券 |
| member | order | 我的订单 |
| cart | product | 查询 SKU 信息、销售属性、最新价 |
| seckill | coupon | 拉取最近三天秒杀场次 |
| seckill | product | 上架时查询 SKU 详情 |
| order | member | 收货地址 |
| order | cart | 选中购物项 |
| order | ware | 库存校验 / 运费 / 锁定库存 |
| order | product | SPU 信息 |
| order | third-party | 支付模板 |
| ware | order | 解锁库存时查询订单状态 |
| ware | member | 计算运费（收货地址） |
| ware | product | 入库时填充 SKU 名称 |

> 秒杀建单为**异步解耦**：seckill 不直接 Feign 调 order，而是发 MQ 消息，由 order 的监听器消费建单。

---

## 3. 统一网关与基础设施模块

### 3.1 网关（gulimall-gateway）

基于 **Spring Cloud Gateway**（WebFlux 响应式），是所有前端请求的统一入口。

**路由规则（`application.yml`）**：

- **路径路由（/api 前缀，面向后台/前端工程）**：`RewritePath` 去掉 `/api` 前缀后转发。
  - `/api/product/**` → `lb://gulimall-product`
  - `/api/member/**` → `lb://gulimall-member`
  - `/api/coupon/**` → `lb://gulimall-coupon`
  - `/api/order/**` → `lb://gulimall-order`
  - `/api/ware/**` → `lb://gulimall-ware`
  - `/api/thirdparty/**` → `lb://gulimall-third-party`
  - `/api/**` → `lb://renren-fast`（后台管理；**注意**：`renren-fast` 后端服务未包含在本仓库 `docker-compose.yml` 与模块清单中，通常作为独立后台工程（renren.io 脚手架）另行部署，此处仅声明转发目标）
- **域名路由（Host 谓词，面向浏览器）**：
  - `gulimall.com`、`item.gulimall.com` → product
  - `search.gulimall.com` → search
  - `auth.gulimall.com` → auth-server
  - `cart.gulimall.com` → cart
  - `order.gulimall.com` → order
  - `member.gulimall.com` → member
  - `seckill.gulimall.com` → seckill

**跨域（CORS）**：`GulimallCorsConfiguration` 注册响应式 `CorsWebFilter`，全局放行 `/**`，允许所有来源/头/方法并 `setAllowCredentials(true)`，避免各服务重复配置。

**服务发现**：`@EnableDiscoveryClient` + Nacos；`lb://服务名` 通过 Nacos 动态寻址与负载均衡，无需写死 IP。

> 说明：当前网关**未实现自定义 GlobalFilter 鉴权**，鉴权依赖各服务自身拦截器校验 Session。Sentinel 网关限流配置（`SentinelGatewayConfig`）已被注释禁用。

**nginx 配合（`./nginx/conf.d/gulimall.conf`）**：外层 nginx 监听 80，按域名 `proxy_pass` 到网关（upstream `gulimall`），并对 `/static/` 做动静分离；`/payed/` 配置用于支付异步回调（支付宝回调用 `proxy_set_header Host order.gulimall.com` 透传 Host）。

---

## 4. 认证与单点登录模块（auth-server）

### 4.1 核心业务功能

- 账号密码注册（带验证码）、账号密码登录
- 微博 OAuth2 社交登录、微信扫码登录
- 单点登录（Session 通过 Redis 在父域共享）
- 短信验证码发送（带防刷）
- 退出登录

### 4.2 关键实现

**账号密码登录（`LoginController.login`）**
1. `MemberFeignService.login(vo)` 远程校验（`POST /member/member/login`）；
2. 返回码为 0 时取出 `MemberResponseVo`，写入 `session.setAttribute(AuthServerConstant.LOGIN_USER, data)`；
3. 重定向到 `return_url`（默认 `http://gulimall.com`）。

**注册（`LoginController.register`）**
1. `@Valid` 校验 `UserRegisterVo`（用户名 6-18、密码 6-18、手机号正则）；
2. 从 Redis 取 `sms:code:phone` 比对验证码；
3. `MemberFeignService.register(vos)`；成功后删除验证码并重定向。

**短信验证码（`LoginController.sendCode`）**
- 接口防刷：同手机号 **60 秒内**重复请求返回异常；
- 生成 6 位随机码，存 Redis **10 分钟**（值含时间戳）；`ThirdPartFeignService.sendCode` 调第三方发短信。

**微博 OAuth2（`OAuth2Controller`）**
1. 前端跳转微博授权页；
2. 回调 `GET /oauth2.0/weibo/success?code=…`，用 `code` 调 `https://api.weibo.com/oauth2/access_token` 换 `access_token/uid`；
3. `MemberFeignService.oauthLogin` → member 服务按 `socialUid` 查重，未注册先自动注册再登录；
4. 成功写 Session 并重定向首页。

**微信扫码（`WxApiController`）**
- 构造微信授权 URL（`open.weixin.qq.com/connect/qrconnect`），`state` 防 CSRF，`redirect_uri` 经 `URLEncoder` 编码；
- 回调用 `code` 换 `access_token/openid`，再 `MemberFeignService.weixinLogin`。

### 4.3 单点登录（SSO）核心机制

`GulimallSessionConfig` + 启动类 `@EnableRedisHttpSession`（`session.timeout=30m`）：
- `cookieSerializer()` 将 Cookie 域名放宽为父域 **`gulimall.com`**、统一命名为 **`GULISESSION`** → 所有子域（auth/cart/product/order…）共享同一 Session；
- 序列化器 `GenericJackson2JsonRedisSerializer`，JSON 序列化便于跨服务读取 `MemberResponseVo`。

### 4.4 数据流转（登录鉴权）

```
浏览器 → auth.gulimall.com/login
  → MemberFeignService.login → 网关 → member 校验
  → auth 写 Session(loginUser) 到 Redis（Cookie 域 gulimall.com）
  → 重定向 gulimall.com
后续访问各子域携带 GULISESSION Cookie → 各服务从 Redis 读同一 Session → SSO 生效
```

### 4.5 技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| 分布式 Session / 单点登录 | Spring Session + Redis，Cookie 域放大到 `gulimall.com` |
| 微服务间 Feign 丢失登录态 | `RequestInterceptor`（`GuliFeignConfig`）透传原请求 `Cookie` 头，下游从 Redis 取同一 Session |
| OAuth2 回调 | 授权页 → 回调 `code` → 服务端换 token → 会员按 `socialUid` 查重/自动注册 → 写 Session |
| 短信防刷 | Redis 存 `sms:code:phone=code_timestamp`，60 秒限频，10 分钟有效，注册成功即删 |
| 跨域 | 网关层统一 `CorsWebFilter` |

---

## 5. 公共模块（common）与第三方服务（third-party）

### 5.1 公共模块（gulimall-common）

被所有微服务以 Maven 依赖引入，提供：
- **统一返回 `R`**：继承 `HashMap`，含 `code/msg/data`，`setData/getData(TypeReference)` 用 fastjson 序列化/反序列化；
- **错误码 `BizCodeEnum`**：5 位（`11` 商品 / `12` 订单 / `13` 购物车 / `14` 物流 / `15` 用户 / `21` 库存 …），`RRException` 自定义异常；
- **分页 `PageUtils` / `Query`**：基于 MyBatis-Plus `Page`，`SQLFilter.sqlInject` 防注入；
- **工具 `HttpUtils`、`SQLFilter`、`HTMLFilter`（XSS）**；
- **共享 VO / TO**：`MemberResponseVo`、`to/mq/`（MQ 传输对象）、`es/SkuEsModel`（ES 文档模型）；
- **常量**：`Constant`、`AuthServerConstant`、`ProductConstant`、`WareConstant`、`OrderConstant` 等。

### 5.2 第三方服务（gulimall-third-party）

封装对阿里云能力的调用，供其他服务经 Feign 调用：

- **OSS 上传**：`OssController.policy`（`GET /oss/policy`）生成 **PostObject 服务端签名直传策略**（accessid/policy/signature/dir/host/expire），前端凭签名直传 OSS，避免前端暴露 AccessKey；
- **短信发送**：`SmsSendController.sendCode` + `SmsComponent` 调阿里云市场短信接口（带 `Authorization: APPCODE`）。

**被调用方**：auth（`ThirdPartFeignService` → `/sms/sendCode`）、order（`ThridFeignService`，注：类名源码拼写为 `Thrid`，用于支付短信等）、网关 `/api/thirdparty/**` 暴露 OSS 策略。

---

## 6. 商品模块（product）与检索模块（search）

### 6.1 商品模块（gulimall-product，端口 10000）

**核心业务**：电商商品域的全量 CRUD 与编排。

**关键实体（库 `gulimall_pms`）**：

| 实体 | 表 | 说明 |
|------|----|------|
| `SpuInfoEntity` | `pms_spu_info` | SPU 主表，`publishStatus`（0 新建 / 1 上架 / 2 下架） |
| `SpuInfoDescEntity` | `pms_spu_info_desc` | SPU 描述图集 |
| `SpuImagesEntity` | `pms_spu_images` | SPU 图集 |
| `SkuInfoEntity` | `pms_sku_info` | SKU 主表（价格、默认图、销量） |
| `SkuImagesEntity` | `pms_sku_images` | SKU 图片 |
| `SkuSaleAttrValueEntity` | `pms_sku_sale_attr_value` | SKU 销售属性值 |
| `AttrEntity` | `pms_attr` | 属性定义（`searchType` 可检索、`attrType` 基本/销售） |
| `AttrGroupEntity` / `AttrAttrgroupRelationEntity` | `pms_attr_group` / `pms_attr_attrgroup_relation` | 属性分组与关联 |
| `ProductAttrValueEntity` | `pms_product_attr_value` | SPU 基本属性值 |
| `CategoryEntity` | `pms_category` | 三级分类（自关联树 `parentCid`） |
| `BrandEntity` | `pms_brand` | 品牌 |
| `CategoryBrandRelationEntity` | `pms_category_brand_relation` | 品牌-分类关联（冗余名，便于查询） |

**核心方法**：
- `SpuInfoServiceImpl.savesupInfo(SpuSaveVo)`（*源码方法名拼写为 `savesupInfo`*）：标记 `@GlobalTransactional` 保存 SPU 全量信息（基础/描述/图片/基本属性/积分 → 远程 coupon；SKU 及图片/销售属性/优惠满减 → 远程 coupon）。**注意**：本仓库 `application.yml`/`bootstrap.yml` 中 `spring.cloud.alibaba.seata.enabled=false`，Seata 未启用，该注解实际不生效，仅对 product 本地 DB 操作有意义，跨服务的 coupon 调用一致性并不由它保障（见第 11 / 14 章）。
- `SpuInfoServiceImpl.up(Long spuId)`：**商品上架** —— 组装 ES 模型，远程调用 search 服务批量写入，成功后改状态为已上架；
- `SkuInfoServiceImpl.item(Long skuId)`：用 `CompletableFuture` + 自定义线程池异步组装商品详情页 7 项数据；
- `CategoryServiceImpl.getCatalogJson()`：三级分类菜单（带多级缓存 + 分布式锁）。

### 6.2 检索模块（gulimall-search，端口 12000）

- `SearchController`（`/list.html`）：接收 `SearchParam`，调 `MallSearchService.search`，结果塞 `Model` 渲染 Thymeleaf 页面；
- `ElasticSaveController`（`/search/save/product`）：接收商品上架的 `List<SkuEsModel>`，批量写入 ES；
- `MallSearchServiceImpl`：用 High Level Rest Client 构建 DSL（bool 查询、过滤、排序、分页、高亮、聚合），并解析结果。

**ES 文档模型 `SkuEsModel`（存于 common）**：每个 SKU 一条文档，含 `skuId/spuId/skuTitle/skuPrice/skuImg/saleCount/hasStock/catalogId/brandId/attrs[]` 等。

### 6.3 数据流转（商品上架 → ES）

```
product 后台保存 SPU/KU
  → /product/spuinfo/{spuId}/up
  → SpuInfoServiceImpl.up：查询 SKU、库存（WMS）、品牌、分类、属性，
     组装 List<SkuEsModel>
  → SearchFeignService（product 调 search）/search/save/product
  → ES 写入索引 gulimall_product
  → 成功：pms_spu_info.publishStatus = 1（已上架）

用户访问 search.gulimall.com/list.html?keyword=...
  → SearchController → MallSearchServiceImpl.search
  → 构建 bool 查询（关键词 must，分类/品牌/属性 filter）
  → 聚合（分类/品牌/属性），高亮 skuTitle，分页
  → 渲染 list 页面
```

### 6.4 技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| SPU/SKU 模型设计 | SPU 描述「是什么商品」，SKU 描述「具体可售单元（规格组合 + 价格 + 库存）」，二者 1:N |
| 商品检索 DSL | bool 查询 + filter + 聚合 + 高亮 + 分页，属性以 nested/扁平字段检索 |
| 三级分类高并发读取 | 多级缓存 + Redisson 分布式锁（防缓存击穿/重建风暴） |
| 商品上架一致性 | 上架成功后本地改状态；search 服务幂等写入（先删后插或按 skuId upsert） |
| 异步组装详情页 | `CompletableFuture` + 自定义线程池并发查询，性能优于串行 |

---

## 7. 会员模块（member）与优惠券模块（coupon）

### 7.1 会员模块（gulimall-member）

**核心业务**：注册、账号密码登录、微博/微信社交登录、会员等级、收货地址、积分/成长值、收藏、登录日志、我的订单（远程调 order）。

**关键实体（库 `gulimall_ums`）**：
- `MemberEntity`（`ums_member`）：`levelId, username, password(BCrypt), nickname, mobile, integration(积分), growth(成长值), socialUid, accessToken, expiresIn`；
- `MemberLevelEntity`（`ums_member_level`）：等级、成长值门槛、免运费等特权。

**关键实现**：
- `MemberServiceImpl.register`：BCrypt 加密 + 用户名/手机号唯一性校验；
- `login(MemberUserLoginVo)` / `login(SocialUser)`（微博）/ `login(String)`（微信）：按 `socialUid` 查重，未注册先自动注册；
- `LoginUserInterceptor`：校验 Session 登录态，写入 `ThreadLocal<MemberResponseVo>`；
- `MemberWebController`：`/memberOrder.html` 经 Feign 调订单服务查我的订单。

### 7.2 优惠券模块（gulimall-coupon，库 `gulimall_sms`）

**核心业务**：优惠券 CRUD、领取历史、满减/打折/会员价、秒杀场次与商品关联维护。

**关键实体**：
- `CouponEntity`（`sms_coupon`）：`couponType`（0 全场赠券 / 1 会员赠 / 2 购物赠 / 3 注册赠）、`useType`（0 全场 / 1 指定分类 / 2 指定商品）、`perLimit`、`minPoint`、`publishCount/receiveCount/useCount`、`enableStartTime/EndTime`、`publish`；
- `CouponHistoryEntity`（`sms_coupon_history`）：领取/使用记录，`useType`（0 未用 / 1 已用 / 2 过期），`orderId/orderSn` 关联订单；
- 商品级优惠三件套：`SkuLadderEntity`（阶梯打折）、`SkuFullReductionEntity`（满减）、`MemberPriceEntity`（会员价），按 `skuId` 关联；
- `SeckillSessionEntity`（`sms_seckill_session`）：场次 `name, startTime, endTime, status`；
- `SeckillSkuRelationEntity`（`sms_seckill_sku_relation`）：`skuId, seckillPrice, seckillCount(总量), seckillLimit(每人限购)`。

**关键方法**：
- `SeckillSessionServiceImpl.getLates3DaySession()`：查询 `startTime` ∈ [今天 00:00, 后天 23:59] 的场次并关联秒杀商品 —— **秒杀上架的数据来源**；
- `SkuFullReductionServiceImpl.saveSkuReduction()`：商品上架时由 product 远程调用，写入阶梯价/满减/会员价三表，供下单结算。

### 7.3 数据流转（优惠券与订单结算关系）

- 商品上架：`product` → `coupon/saveSkuReduction` 落库优惠三件套；
- 领券：`CouponHistoryEntity` 记录 `getType`（0 后台赠 / 1 主动领）、`memberId`；
- 使用/结算：`CartVo.totalAmount` + `reduce` 字段；结算时由订单/购物车侧读取「选中商品 + 可用优惠券」计算优惠；`CouponHistoryEntity.orderSn` 关联订单标记已用。

---

## 8. 购物车模块（cart）

### 8.1 核心业务

基于 Redis 的纯前端渲染购物车（**无独立数据库表**）：添加、查询、勾选/取消、改数量、删除、结算取已选中项、登录时合并临时购物车。

### 8.2 数据结构（Redis）

- **Key**：`gulimall:cart:{userId}`（登录）或 `gulimall:cart:{userKey}`（未登录），均为 **Hash**；
- **field** = `skuId`，**value** = `CartItemVo` 的 JSON；
- `CartItemVo`：`skuId, check(true), title, image, skuAttrValues, price, count, totalPrice(实时 count*price)`；
- `CartVo`：`items, countNum, countType, totalAmount(仅累加勾选项), reduce`；
- `UserInfoTo`：`userId, userKey, tempUser`。

### 8.3 数据流转（登录/未登录处理）

1. `CartInterceptor.preHandle`：
   - 从 Session 取 `LOGIN_USER`（SpringSession 共享）→ 设 `userId`；
   - 读 Cookie `user-key` → 设 `userKey`；都没有则 `UUID` 生成；
   - `postHandle`：非临时用户时下发 `user-key` Cookie（domain=`gulimall.com`，30 天）。
2. `getCartOps`：有 `userId` 用登录 Key，否则用临时 Key；
3. `addToCart`：`BoundHashOperations` 操作 Hash；不存在则异步（线程池）远程查 `product.getInfo` 与 `getSkuSaleAttrValues` 写入；已存在则累加 `count`；
4. `getCart`（登录态）：先查临时 Key，逐项合并到登录 Key（累加数量），再清除临时 Key。

### 8.4 技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| 登录/未登录混合 | 拦截器双重身份（Session 登录态 + Cookie 临时态） |
| 购物车合并 | 登录后自动将临时 Key 合并进登录 Key 并清除 |
| 异步查 SKU 信息 | `CompletableFuture.runAsync(..., executor)` 并发查询后聚合 |
| 库存/价格实时性 | 下单时以 product 最新价为准，购物车展示价仅参考 |

---

## 9. 订单模块（order）与仓储模块（ware）

### 9.1 订单模块（gulimall-order，库 `gulimall_oms`）

**核心业务**：订单确认页、提交下单、状态流转、支付回调、超时关闭、秒杀建单。

**关键实体**：
- `OrderEntity`（`oms_order`）：`order_sn, member_id, status, total_amount, pay_amount, freight_amount, coupon/promotion/integration_amount, auto_confirm_day, confirm_status, delete_status`；
- `OrderItemEntity`（`oms_order_item`）：`order_sn, spu_id, sku_id, sku_quantity, real_amount, gift_integration, gift_growth, sku_attrs_vals`；
- `OrderOperateHistoryEntity`、`PaymentInfoEntity`、`OrderReturnApplyEntity` 等（售后/支付/退货）。

**订单状态枚举 `OrderStatusEnum`**：0 待付款 / 1 已付款 / 2 已发货 / 3 已完成 / 4 已取消 / 5 售后中 / 6 售后完成。

### 9.2 仓储模块（gulimall-ware，库 `gulimall_wms`）

**核心业务**：库存查询、锁定/解锁、入库、运费计算、采购。

**关键实体**：
- `WareSkuEntity`（`wms_ware_sku`）：`sku_id, ware_id, stock(库存), stock_locked(锁定库存)`；
- `WareInfoEntity`（`wms_ware_info`）：仓库名/地址/区域；
- `WareOrderTaskEntity` / `WareOrderTaskDetailEntity`：库存工作单 + 明细（`lock_status` 1 锁定 / 2 已解锁）—— **锁定/解锁的追溯依据**；
- `PurchaseEntity` / `PurchaseDetailEntity`：采购单与明细。

### 9.3 数据流转（下单 → 库存 → 释放）

```
OrderWebController.submitOrder
  → OrderServiceImpl.submitOrder（事务方法）
    1. Lua 脚本原子校验 + 删除 Redis 防重令牌
    2. createOrder()：生成 orderSn（IdWorker）、构建订单项、计算价格
       - builderOrderItem 远程查 product 的 SPU 信息
       - builderOrder 远程查 wms.getFare 运费 + 收货地址
    3. 金额校验（与页面 payPrice 误差 < 0.01）
    4. saveOrder() 保存订单 + 订单项
    5. 封装 WareSkuLockVo → wmsFeignService.orderLockStock() 远程锁库存
       - 成功 → 发 MQ「order.create.order」→ 返回支付页
       - 失败（NoStockException）→ 订单事务回滚
    6. 删除购物车已选中项（Redis Hash）
```

**库存锁定 `WareSkuServiceImpl.orderLockStock`**：
1. 保存 `wms_ware_order_task` 工作单；
2. 对每个 SKU 找有货仓库列表；
3. 遍历仓库执行锁库存 SQL，成功保存明细（`lock_status=1`）；
4. 每锁定一项发 `stock.locked` 到 `stock-event-exchange`。

**防超卖核心 SQL**：
```sql
UPDATE wms_ware_sku SET stock_locked = stock_locked + #{num}
WHERE sku_id=#{skuId} AND ware_id=#{wareId} AND stock - stock_locked > 0
```
返回值=影响行数（1 成功 / 0 该仓不足），多仓轮询，全失败抛 `NoStockException`。

**库存解锁（两种机制）**：
- **机制 A 消息驱动**：`StockReleaseListener` 监听 `stock.release.stock.queue` 的 `StockLockedTo` → 查明细，订单不存在 / 已取消(4) 且 `lock_status=1` 才解锁；已支付不解锁；远程失败 `basicReject(requeue=true)` 重试；
- **机制 B 订单关闭驱动**：`closeOrder` 关单后发 `order.release.other` → 绑定到 `stock.release.stock.queue` → 按 `orderSn` 解锁所有 `lock_status=1`（防订单服务卡顿致库存永不释放）。

**订单超时关闭（死信队列）**：`submitOrder` 成功后发 `order.create.order` → `order.delay.queue`（TTL=60s，DLX=`order-event-exchange`，DLK=`order.release.order`）→ 过期进入 `order.release.order.queue` → `OrderCloseListener` 关单并触发库存解锁。

**支付回调**：`handlePayResult`/`asyncNotify` 校验订单状态（已支付/已取消则直接返回 success 防重复），支付宝/微信验签后更新状态为已支付(PAYED)。

**RabbitMQ 拓扑（order + ware）**：
- `order-event-exchange`（Topic）：`order.create.order` → `order.delay.queue`；`order.release.order` → `order.release.order.queue`；`order.release.other.#` → `stock.release.stock.queue`；`order.seckill.order` → `order.seckill.order.queue`；
- `stock-event-exchange`（Topic）：`stock.locked` → `stock.delay.queue`；`stock.release.#` → `stock.release.stock.queue`；
- 统一 `Jackson2JsonMessageConverter` 做 JSON 序列化。

### 9.4 技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| 分布式事务（订单+库存一致性） | 放弃 Seata（高并发加锁、并行退化），采用**本地事务 + MQ 最终一致性**：先本地存订单 → 远程锁库存 → 失败回滚；锁成功即发 MQ 保障后续 |
| 库存超卖 | `stock - stock_locked > 0` 行级原子 SQL + 多仓轮询；DB 行锁保障 |
| 订单超时关闭 | 死信队列（TTL 60s）自动关单，并联动库存解锁 |
| 幂等/防重 | Redis Lua 原子令牌（防重复提交）；工作单 `lock_status` 状态机（防重复解锁）；支付回调状态校验 |
| 可靠消息 | 锁库存后立即发消息；手动 ACK，失败 requeue；工作单可追溯补解锁 |
| 异步丢请求头 | `RequestContextHolder` 在线程池任务共享请求头 + `ThreadLocal` 传登录会员 |
| 线程复用串号 | `ThreadLocal<OrderSubmitVo>` 在 `finally` 中 remove |

> 备注：`MySeataConfig` 已声明但默认关闭（需 `seata.enabled=true` 才启用 DataSourceProxy），与代码注释「高并发不推荐 Seata」一致。

---

## 10. 秒杀模块（seckill）

### 10.1 核心业务

定时上架最近三天秒杀商品到 Redis、查询可秒杀商品、商品详情秒杀信息、执行秒杀（校验 → 信号量扣库存 → 发 MQ 建单）。

### 10.2 关键类

- `SeckillScheduled`：`@Scheduled(cron="0 0 1/1 * * ?")` 每小时触发，加 Redisson 分布式锁 `seckill:upload:lock` 后上架；
- `SeckillServiceImpl`：`uploadSeckillSkuLatest3Days`、`getCurrentSeckillSkus`、`getSkuSeckilInfo`、`kill`；
- `SeckillController`（`@RestController`）：`/getCurrentSeckillSkus`、`/sku/seckill/{skuId}`、`/kill`。

### 10.3 Redis 数据结构

- **List** `seckill:sessions:{startTime}_{endTime}`：push `sessionId-skuId` 列表；
- **Hash** `seckill:skus`：field=`sessionId-skuId`，value=`SeckillSkuRedisTo` JSON（含 `startTime, endTime, randomCode`）；
- **信号量** `seckill:stock:{randomCode}`：Redisson `RSemaphore`，初始许可 = 秒杀总量。

### 10.4 数据流转（上架 → 秒杀 → 建单）

**上架（定时）**：
1. `SeckillScheduled` 加分布式锁 `seckill:upload:lock`（Redisson，幂等上架）；
2. `SeckillServiceImpl` → `couponFeignService.getLates3DaySession()` 拉最近三天场次 + 商品；
3. `saveSessionInfos` 写入 `seckill:sessions:{start}_{end}`（List）；
4. `saveSessionSkuInfo` 写入 `seckill:skus`（Hash）；
5. 对每个商品 `redissonClient.getSemaphore("seckill:stock:"+randomCode).trySetPermits(seckillCount)` 建立**分布式信号量=库存**。

**秒杀下单 `kill`**：
1. 取登录用户；从 `seckill:skus` 取商品；
2. 校验时间在 `[startTime, endTime]`；校验 `randomCode` 与 `killId` 一致（防 URL 猜测）；
3. 校验购买数 ≤ `seckillLimit` 且 ≤ 信号量余量；
4. **幂等**：`setIfAbsent(userId-skuId, num, ttl)`（SETNX），占位成功才算首次购买；
5. `semaphore.tryAcquire(num, 100ms)` 扣库存；
6. 生成 `orderSn=IdWorker.getTimeId()`，组装 `SeckillOrderTo`，`rabbitTemplate.convertAndSend("order-event-exchange","order.seckill.order",orderTo)`。

**MQ → 订单**：`OrderSeckillListener`（`@RabbitListener(queues="order.seckill.order.queue")`）收到 `SeckillOrderTo` → `createSeckillOrder`：落 `OrderEntity`（`orderSn, memberId, payAmount=seckillPrice*num, status=CREATE_NEW`）+ `OrderItemEntity`，手动 ack，异常 `requeue`。

### 10.5 技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| 秒杀防超卖 | Redisson `RSemaphore`（初始许可=总量）`tryAcquire` 原子扣减；上架加分布式锁幂等 |
| 防恶意刷/URL 猜测 | 每商品生成 UUID `randomCode`，仅活动期存在，`kill` 校验一致 |
| 用户重复购买 | `setIfAbsent(userId-sessionId-skuId, num, ttl)` SETNX 占位 |
| 高并发下单解耦 | 秒杀成功仅发 MQ，order 异步建单（接口 ~10ms）；手动 ack 保证不丢单 |
| 热点数据读取/限流 | `redisTemplate.keys(SESSION_PREFIX+"*")` 定位场次 + 批量 `multiGet`；Sentinel `@SentinelResource` 限流被限返回 null |

---

## 11. 全局技术难点与解决方案汇总

| 领域 | 难点 | 解决方案 |
|------|------|----------|
| 服务治理 | 服务发现 / 动态路由 | Nacos 注册中心 + 网关 `lb://服务名` 负载均衡 |
| 跨域 | 浏览器跨域 | 网关统一 `CorsWebFilter` 放行 |
| 认证 | 分布式 Session / SSO | Spring Session + Redis，Cookie 域放大到父域 `gulimall.com` |
| 认证 | 微服务间丢登录态 | Feign `RequestInterceptor` 透传 `Cookie` 头 |
| 安全 | OAuth2 回调 | 授权页 → code → 服务端换 token → 按 `socialUid` 查重/自动注册 → 写 Session |
| 安全 | SQL 注入 / XSS | `SQLFilter.sqlInject` 过滤排序字段；`HTMLFilter` 转义 |
| 安全 | OSS 密钥暴露 | 服务端生成 PostObject 签名，前端直传 |
| 商品 | SPU/SKU 设计 | SPU=商品定义，SKU=可售规格单元，1:N |
| 检索 | 复杂检索 | ES bool + filter + 聚合 + 高亮 |
| 缓存 | 缓存击穿/重建 | 多级缓存 + Redisson 分布式锁 |
| 下单 | 分布式事务 | 本地事务 + MQ 最终一致性（放弃 Seata） |
| 库存 | 超卖 | `stock - stock_locked > 0` 行锁 SQL + 多仓轮询 |
| 订单 | 超时关闭 | 死信队列 TTL 自动关单 + 联动解锁库存 |
| 幂等 | 重复提交/解锁/支付 | Redis Lua 令牌、工作单状态机、支付状态校验 |
| 秒杀 | 超高并发 | Redisson 信号量扣减 + 分布式锁上架 + MQ 异步建单 + 幂等 SETNX |
| 性能 | 串行远程调用 | `CompletableFuture` + 自定义线程池并发编排 |
| 限流 | 突发流量 | Sentinel `@SentinelResource` 限流 |

---

## 12. 中间件使用汇总

| 中间件 | 使用模块 | 用途 |
|--------|----------|------|
| **Nacos** | 全部 | 服务注册发现（8848）+ 配置中心 |
| **Redis** | 全部（Session） | 分布式 Session、购物车、防重令牌、秒杀信号量/库存、三级分类缓存 |
| **Redisson** | product / seckill | 分布式锁（上架/分类）、`RSemaphore` 秒杀库存信号量 |
| **RabbitMQ** | order / ware / seckill | 订单创建/释放、库存锁定/解锁、订单超时死信、秒杀建单 |
| **Elasticsearch** | product / search | 商品上架索引、全文检索、聚合、高亮 |
| **Spring Session** | auth / cart / member / ... | 子域 Session 共享（SSO） |
| **OSS** | third-party / product | 图片/文件直传 |
| **Sentinel** | seckill（及其它） | 限流（秒杀列表） |
| **MySQL** | 各业务模块 | 业务数据持久化（pms/oms/wms/ums/sms/coupon） |

---

## 附：关键代码路径索引

- 网关：`gulimall-gateway/.../GulimallCorsConfiguration.java`、`application.yml`
- 认证：`gulimall-auth-server/.../controller/{LoginController,OAuth2Controller,WxApiController}.java`、`GulimallSessionConfig.java`
- 公共：`gulimall-common/.../utils/R.java`、`exception/BizCodeEnum.java`、`es/SkuEsModel.java`
- 第三方：`gulimall-third-party/.../controller/{OssController,SmsSendController}.java`
- 商品：`gulimall-product/.../service/impl/{SpuInfoServiceImpl,SkuInfoServiceImpl,CategoryServiceImpl}.java`
- 检索：`gulimall-search/.../service/impl/MallSearchServiceImpl.java`、`ElasticSaveController.java`
- 会员：`gulimall-member/.../service/impl/MemberServiceImpl.java`、`interceptor/LoginUserInterceptor.java`
- 优惠券：`gulimall-coupon/.../service/impl/{SeckillSessionServiceImpl,SkuFullReductionServiceImpl}.java`
- 购物车：`gulimall-cart/.../{controller/CartController,service/impl/CartServiceImpl,interceptor/CartInterceptor}.java`
- 订单：`gulimall-order/.../service/impl/OrderServiceImpl.java`、`listener/{OrderCloseListener,OrderSeckillListener}.java`、`config/MyRabbitMQConfig.java`
- 仓储：`gulimall-ware/.../service/impl/WareSkuServiceImpl.java`、`listener/StockReleaseListener.java`、`resources/mapper/ware/WareSkuDao.xml`
- 秒杀：`gulimall-seckill/.../{scheduled/SeckillScheduled,service/impl/SeckillServiceImpl}.java`、`controller/SeckillController.java`

> 本文档基于源码静态分析生成，部分业务（如优惠券领取/结算优惠金额完整链路）在源码中以基础 CRUD 呈现，核心结算逻辑位于订单/购物车侧，必要时可进一步深入 `OrderServiceImpl` 与 `CartServiceImpl` 跟踪。

---

## 13. 开发问题 QA（常见问题与解决方案）

> 本章以问答形式，总结本项目开发与运行期高频遇到的问题、根因与解决手段，均可在前述模块源码中得到印证。

### Q1：浏览器访问各子域接口报跨域（CORS）错误，前端拿不到响应？

**根因**：前端（Vue / Thymeleaf）与后端微服务不同源（域名/端口不同），浏览器预检（OPTIONS）被拦截；各服务若各自配 `@CrossOrigin` 易遗漏且冲突。

**解决方案**：统一在**网关层** `GulimallCorsConfiguration` 用响应式 `CorsWebFilter` 全局放行（允许所有来源/头/方法 + `setAllowCredentials(true)`），业务服务不再单独处理。注意网关是 WebFlux，不能用 `@CrossOrigin` 注解方案。

---

### Q2：服务 A 通过 Feign 调服务 B 后，B 里取不到登录用户（Session 丢了）？

**根因**：Feign 发起的是**新 HTTP 请求**，不会自动携带浏览器原始的 `Cookie`（含 `GULISESSION`），下游服务从 Redis 读不到同一 Session，导致 `LoginUserInterceptor` / `CartInterceptor` 取不到用户。

**解决方案**：在调用方配置 `RequestInterceptor`（`member/config/GuliFeignConfig.java`、`order/config/GuliFeignConfig.java` 的 `@Bean("requestInterceptor")`），从 `RequestContextHolder.getRequestAttributes()` 取出原请求的 `Cookie` 头，写入 Feign `RequestTemplate` 的 `Cookie`。这样下游服务能还原 Session 实现「登录态透传」。

---

### Q3：在 `CompletableFuture` 异步线程池里发 Feign 请求，下游取不到用户甚至空指针？

**根因**：`RequestContextHolder` 默认用 `ThreadLocal` 存请求上下文，异步线程**不继承**主线程的 RequestAttributes；同时自定义线程池里的 `ThreadLocal<MemberResponseVo>` 也需手动传递。

**解决方案**：
- 主线程在提交异步任务前，先 `RequestContextHolder.getRequestAttributes()` 取出 attributes，在异步任务内 `RequestContextHolder.setRequestAttributes(attributes)` 重新绑定（order 的 `confirmOrder()` 即此做法）；
- 登录会员信息用 `ThreadLocal` 在拦截器存入，异步任务显式读取并传递，避免依赖线程池复用导致的脏数据。

---

### Q4：下单时订单库写成功但库存没扣，或库存扣了订单回滚，数据不一致？

**根因**：下单涉及 **order 库（oms）+ ware 库（wms）** 的跨库写，传统本地事务无法跨越；用 Seata 在高峰期又会加锁、并行退化，性能差。

**解决方案**：**放弃强一致，改用本地事务 + MQ 最终一致性**：
1. `submitOrder` 内先本地事务保存订单（失败直接回滚）；
2. 再远程 `orderLockStock` 锁库存，失败抛 `NoStockException` 使订单回滚；
3. 锁库存成功后立即发 MQ「`order.create.order`」，即便后续订单服务异常，库存侧也已锁定并通过消息 / 工作单追溯解锁，最终一致。

> 代码 `MySeataConfig` 已声明但默认关闭；在 `gulimall-product` 的 `application.yml` / `bootstrap.yml` 中 `spring.cloud.alibaba.seata.enabled=false` 亦已确认关闭，与「高并发不推荐 Seata」一致。

---

### Q5：高并发秒杀/下单，库存出现超卖（扣成负数）？

**根因**：多请求同时 `SELECT` 再 `UPDATE` 非原子；或先判断库存再扣减存在时间窗竞争。

**解决方案**（ware 的 `WareSkuDao.lockSkuStock`）：
```sql
UPDATE wms_ware_sku SET stock_locked = stock_locked + #{num}
WHERE sku_id=#{skuId} AND ware_id=#{wareId} AND stock - stock_locked > 0
```
- DB 行级原子：条件 `stock - stock_locked > 0` 在一条 `UPDATE` 内判断并扣减，返回影响行数（1 成功 / 0 不足）；
- 多仓库轮询：当前仓不足尝试下一仓，全部失败才抛 `NoStockException`；
- 秒杀场景额外用 **Redisson `RSemaphore`**（`seckill:stock:{randomCode}`，初始许可=总量）做原子扣减，与 DB 双重保障。

---

### Q6：用户下单后一直不支付，库存被永久锁死？

**根因**：库存锁定后只改 `stock_locked`，若订单服务卡顿/宕机，没有触发解锁，库存永远不可用。

**解决方案**：**双保险解锁机制**：
- **订单超时关闭（死信队列）**：`submitOrder` 成功后发 `order.create.order` 到 `order.delay.queue`（TTL=60s，DLX=`order-event-exchange`，DLK=`order.release.order`），过期进入 `order.release.order.queue` → `OrderCloseListener.closeOrder` 关单，并额外发 `order.release.other` → 绑定到 `stock.release.stock.queue` → `unlockStock(OrderTo)` 按 `orderSn` 解锁所有 `lock_status=1` 的明细。
- **消息驱动解锁**：库存锁定后立即发 `stock.locked` → `StockReleaseListener` 收 `StockLockedTo`，查明细与订单状态，订单不存在/已取消且 `lock_status=1` 才解锁，已支付不解锁。

---

### Q7：重复点击提交订单，产生了两笔订单？

**根因**：前端重复提交 / 网络重试导致同一请求多次到达。

**解决方案**：**防重令牌（幂等）**
- 进入订单确认页时生成 UUID 令牌存入 Redis（`order:token:{userId}`）并下发页面隐藏域；
- 提交时用 **Lua 脚本原子校验并删除令牌**（`if redis.call('get',key)==token then return redis.call('del',key) end`），校验失败直接拒绝。

---

### Q8：未登录加了购物车，登录后购物车商品「不见了」？

**根因**：未登录用临时 Cookie `user-key` 存 `gulimall:cart:{userKey}`，登录后用 `userId` 存 `gulimall:cart:{userId}`，两份 Key 不互通。

**解决方案**：`CartInterceptor` 解析双重身份（Session 登录态 + Cookie 临时态）；登录态访问 `getCart` 时，先读临时 Key 数据，逐项合并（数量累加）进登录 Key，再 `clearCartInfo` 清除临时 Key，实现**登录后自动合并**。

---

### Q9：Spring Session 配了 Redis，但各子域（auth/cart/order）仍然互相不认识登录态？

**根因**：默认 Cookie 作用域是子域（如 `auth.gulimall.com`），其它子域读不到；或序列化器默认 JDK，跨服务反序列化 `MemberResponseVo` 失败。

**解决方案**：`GulimallSessionConfig.cookieSerializer()` 将 Cookie 域名放宽为父域 **`gulimall.com`**、统一命名 **`GULISESSION`**（`@EnableRedisHttpSession`，超时 30m），序列化器用 `GenericJackson2JsonRedisSerializer` 以 JSON 存储，保证各子域共享同一 Session（单点登录核心）。

---

### Q10：商品上架后，在 search 搜不到 / 检索结果分类、品牌、属性聚合不出来？

**根因**：多为 ES 索引映射（mapping）字段类型不对、可检索属性未建模、或上架时未把 `attrs` 写入文档；也可能是 `SkuEsModel` 与 ES mapping 字段不匹配。

**解决方案**：
- 上架链路 `SpuInfoServiceImpl.up` 需把 SKU 的销售/基本属性、`catalogId`、`brandId`、`hasStock`、`saleCount` 等完整组装进 `SkuEsModel`；
- `MallSearchServiceImpl` 用 bool 查询 + filter（分类/品牌/属性）+ 聚合（terms agg）+ 高亮（skuTitle）构建 DSL；确保 `attrs` 字段在 ES mapping 中为可被聚合检索的类型；
- 中文检索建议引入 IK 分词器并对 `skuTitle` 指定 `analyzer`；字段名/类型与 mapping 对齐，必要时删除索引重建。

---

### Q11：消息队列里订单/库存消息「丢了」或「重复消费」？

**根因**：默认自动 ACK 下消费者处理中途异常会丢消息；网络抖动导致消息重复投递。

**解决方案**：
- 监听器用**手动 ACK**：处理成功 `basicAck`，失败 `basicReject(requeue=true)` 重新入队（`StockReleaseListener`、`OrderSeckillListener` 均如此）；
- 消费端做**幂等**：库存解锁依赖工作单 `lock_status` 状态机（1 锁定 → 2 已解锁），重复消息因状态已变为 2 而不重复解；订单侧用 `OrderStatusEnum` 校验避免重复改状态；
- 消息统一 `Jackson2JsonMessageConverter` 序列化，绑定关系由 `MyRabbitMQConfig` 显式声明，避免 exchange/queue/routingKey 不匹配导致消息进死信。

---

### Q12：三级分类菜单每次都查数据库，接口慢、DB 压力大？

**根因**：分类树是高频只读数据，未加缓存；直接查库并递归组装。

**解决方案**：`CategoryServiceImpl.getCatalogJson()` 采用**多级缓存 + 分布式锁**：
- 先查 Redis，命中直接返回；
- 未命中时加 **Redisson 分布式锁**（防缓存击穿/「惊群」重建），只放一个线程查库并回写 Redis，其余线程等待后读缓存；
- 注意缓存与 DB 的一致性（分类变更时删除/双写缓存）。

---

### Q13：网关启动报与 Sentinel / WebFlux 相关冲突，或限流不生效？

**根因**：Spring Cloud Gateway 基于 WebFlux，与部分 Sentinel 自动配置 / Servlet 体系不兼容；本项目已主动禁用。

**解决方案**：启动类排除 `Sentinel*AutoConfiguration`，`application.yml` 中 `sentinel.enabled=false`，`SentinelGatewayConfig` 的 `@Configuration` 已被注释；如需限流，在业务侧（如 seckill 的 `getCurrentSeckillSkus`）用 **Sentinel `@SentinelResource` + `SphU.entry` + `blockHandler`** 做方法级限流，被限流返回 null 不崩。

---

### Q14：Docker 部署后 MySQL/ES/RabbitMQ 连不上，或时间显示差 8 小时？

**根因**：容器网络/端口未映射、健康检查未就绪；容器默认时区为 UTC 导致日志与订单时间偏差。

**解决方案**：`docker-compose.yml` 已为各服务设置 `TZ: Asia/Shanghai`，MySQL 用 `utf8mb4` + `mysql_native_password` 认证插件（兼容 8.0 客户端）；ES 单节点 `discovery.type=single-node` + `bootstrap.memory_lock` + `ES_JAVA_OPTS=-Xms512m -Xmx512m` 防 OOM；MySQL 数据卷挂载 + `./db` 初始化 SQL 自动建库建表。注意 ES 版本（7.17）需与 High Level Rest Client 版本匹配。

---

### Q15：OSS 上传报跨域或前端拿到 AccessKey 报错？

**根因**：前端直传若把阿里云 AccessKey 写死在浏览器，既不安全又易因 Bucket 跨域配置缺失而失败。

**解决方案**：`OssController.policy` 由**服务端生成 PostObject 签名直传策略**（accessid/policy/signature/dir/host/expire），前端凭签名直传 OSS（bucket `gulimall-clouds`），绝不暴露 AccessKey；OSS Bucket 控制台需配置允许的跨域来源与 PUT/POST 方法。

---

### Q16：验证码接口被刷（短信轰炸）？

**根因**：发送验证码接口未限频，恶意脚本可循环调用。

**解决方案**：`LoginController.sendCode` 用 Redis 存 `sms:code:{phone} = code_timestamp`：
- 同一手机号 **60 秒内**重复请求直接拒绝（`SMS_CODE_EXCEPTION`）；
- 验证码有效期 **10 分钟**，注册成功即删除；校验失败（与用户输入不符）不通过。

---

### Q17：秒杀接口被恶意 URL 猜测刷单？

**根因**：若秒杀入口 URL 仅含 skuId 可被枚举，攻击者能绕过页面直接高并发请求。

**解决方案**：上架时给每个秒杀商品生成 **UUID `randomCode`**，仅活动期间存在于 Redis（`seckill:skus`）；`kill` 接口校验 `randomCode` 与页面传回的 `killId` 一致，非法请求直接失败；同时用 `seckillLimit` 限制每人购买数 + `setIfAbsent(userId-sessionId-skuId, num, ttl)` 做用户级幂等防重复购买。

---

### 小结：问题归类

| 分类 | 典型问题 | 核心手段 |
|------|----------|----------|
| 跨域/认证 | CORS、Session 丢失、Feign 丢头 | 网关 CORS、SpringSession 父域、RequestInterceptor 透传 |
| 并发/一致性 | 超卖、重复订单、数据不一致 | 行锁 SQL、Lua 防重令牌、MQ 最终一致、死信关单 |
| 异步 | 线程池丢请求上下文 | 手动传递 RequestAttributes / ThreadLocal |
| 缓存 | 击穿、慢查询 | 多级缓存 + 分布式锁（Redisson） |
| 消息 | 丢失/重复 | 手动 ACK + requeue + 工作单状态机幂等 |
| 安全 | 短信刷、URL 猜测、OSS 泄露 | 限频、randomCode、服务端签名直传 |
| 部署 | 时区、版本、网络 | docker-compose TZ、ES 版本匹配、Nacos 发现 |

---

## 14. 已知局限与待完善（源码审视备注）

> 基于源码静态分析发现的、对生产落地或文档严谨性有影响的瑕疵与缺口，供后续完善参考。

1. **分布式事务注解实际未生效**：`gulimall-product` 的 `SpuInfoServiceImpl.savesupInfo` / `up` 标记了 `@GlobalTransactional`，但 `application.yml` 与 `bootstrap.yml` 中 `spring.cloud.alibaba.seata.enabled=false`，Seata 并未启用，注解形同虚设。即便启用，方法内对 coupon 的 Feign 调用也无法被其回滚（跨服务）。当前 SPU 保存的跨服务一致性依赖各服务自身幂等，未做强保障。

2. **SQL 注入风险（字符串拼接）**：`gulimall-ware` 的 `WareSkuDao.xml` 中 `unLockStock` 使用了 `sku_id = ${skuId}` 的 `${}` 直接拼接（同文件 `lockSkuStock` / `addStock` 正确使用了 `#{}` 预编译）。若 `skuId` 来自不可信输入存在注入风险，建议统一改为 `#{}`。

3. **网关层鉴权缺失**：`gulimall-gateway` 未实现自定义 `GlobalFilter` 鉴权，登录态校验分散在各服务 `Interceptor`（依赖 Session/Cookie）。网关仅完成 CORS 与路由；Sentinel 限流也被 `enabled: false` 关闭，无网关级熔断/限流生效。

4. **renren-fast 后台后端不在仓库内**：网关 `admin_route` 将 `/api/**` 转发至 `lb://renren-fast`，但 `renren-fast` 服务未包含在本仓库 `docker-compose.yml` 与模块清单中，需作为独立工程部署，否则后台管理接口不可用（见第 3.1 章注）。

5. **优惠金额结算链路简化**：优惠券「领取—使用—订单结算优惠计算」在源码中多为**基础 CRUD + VO 字段**（如 `CartVo.reduce`、`OrderEntity.couponAmount`），完整的「满减 / 打折 / 会员价叠加 + 最优券选择」计算逻辑未见完整实现，下单时以商品/购物车展示价为主，结算细节需结合业务补完。

6. **源码命名瑕疵**（不影响运行，但影响可读性与检索）：
   - 订单状态枚举包名拼写为 `enume`（应为 `enums`）；
   - 商品保存方法名 `savesupInfo`（应为 `saveSpuInfo`）；
   - `order` 模块 Feign 客户端类名拼写为 `ThridFeignService`（`third` → `Thrid`）；
   - 优惠券场次接口方法 `getLates3DaySession`（应为 `getLatest3DaySession`）。

7. **死信队列 TTL 为演示值**：订单关单死信 `order.delay.queue` 的 TTL 设为 `60000ms`（60 秒），仅用于演示「下单后未支付自动关单」；生产环境应按业务（如 30 分钟）调整，并注意与支付宝 / 微信支付超时窗口对齐。

---

*修订说明：本文档基于仓库源码（截至 2026-07）静态分析生成，端口、网关路由、枚举值、关键 SQL、注解开关等均已对照源码核对；标注「*源码推断*」处为基于命名 / 配置的合理性推断。第 14 章所列局限为源码审视结论。*
