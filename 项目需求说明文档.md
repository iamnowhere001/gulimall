# 谷粒商城（guli-mall）项目需求说明文档

> 文档版本：v1.0
> 编写依据：项目源码（mall-\* 系列微服务模块、renren-fast 后台、renren-fast-vue 前端）及既有技术文档
> 适用对象：产品经理、开发工程师、测试工程师、架构师

***

## 1. 文档概述

### 1.1 目的

本文档在已有技术架构文档的基础上，从「业务需求」视角对谷粒商城项目进行系统性说明，明确系统的目标用户、功能范围、业务能力、核心流程与非功能性约束，作为后续开发、测试与验收的依据。

### 1.2 范围

涵盖商城前台（商品浏览、搜索、购物车、登录、下单、支付）与后台管理（商品、订单、营销、会员、库存、权限）两大体系，以及支撑它们的网关、认证、搜索、第三方集成等微服务。

### 1.3 术语

| 术语       | 说明                                              |
| :------- | :---------------------------------------------- |
| SPU      | Standard Product Unit，标准化产品单元（如「iPhone 13」）     |
| SKU      | Stock Keeping Unit，最小库存单位（如「iPhone 13 128G 黑色」） |
| B2C      | 商家对消费者的电子商务模式                                   |
| SSO      | 单点登录（Single Sign-On）                            |
| OAuth2.0 | 开放授权协议，用于社交登录                                   |
| ES       | Elasticsearch 搜索引擎                              |

***

## 2. 项目背景与目标

### 2.1 背景

谷粒商城是一个基于 Spring Cloud 微服务架构的 B2C 电商平台，采用前后端分离设计，覆盖从商品上架、检索、加购、下单到支付、售后、库存履约的完整电商链路，并配套后台管理系统。

### 2.2 目标

- 提供面向消费者的购物门户（商品检索、详情、购物车、下单、支付）。
- 提供面向运营人员的后台管理能力（商品、订单、营销、会员、库存、权限）。
- 通过微服务拆分实现高内聚、低耦合，具备良好的可扩展性与可维护性。
- 提供高性能的商品全文检索与企业级单点登录能力。

***

## 3. 用户角色

| 角色         | 职责                    | 使用入口                                              |
| :--------- | :-------------------- | :------------------------------------------------ |
| 普通会员（C端用户） | 浏览商品、搜索、加购、下单、支付、查看订单 | 商城前台（gulimall.com / search / item / cart / order） |
| 运营/管理员     | 管理商品、订单、营销活动、会员、库存    | 后台管理系统（renren-fast-vue）                           |
| 系统/开发人员    | 服务治理、配置管理、监控          | Nacos / Sentinel / Kibana 控制台                     |

***

## 4. 总体功能结构

```
谷粒商城
├── 商品中心 (mall-product)
├── 搜索中心 (mall-search)
├── 购物车 (mall-cart)
├── 认证中心 (mall-auth-server)
├── 订单中心 (mall-order)
├── 营销中心 (mall-coupon)
├── 会员中心 (mall-member)
├── 库存中心 (mall-ware)
├── 第三方服务 (mall-third-party)
├── 网关 (mall-gateway)
├── 后台管理 (renren-fast + renren-fast-vue)
└── 公共基础 (mall-common)
```

***

## 5. 各子系统功能需求

### 5.1 商品中心（mall-product）

**职责**：商品全生命周期管理，是商城的核心数据源。

**功能列表**：

- 三级商品分类管理：树形展示、新增、修改、拖拽排序、级联逻辑删除。
- 品牌管理：品牌 CRUD、启停状态管理，支持分组校验。
- 品牌-分类关联：按品牌查分类、按分类查品牌（支撑「分类下展示品牌」）。
- 属性管理：规格参数（基本属性）与销售属性，属性分组（AttrGroup）管理，属性与分组关联维护。详情页按分类聚合「分组+属性」用于规格展示。
- SPU 管理：SPU 基本信息、描述、图片、属性值、SKU、营销信息的完整创建与维护。
- SKU 管理：SKU 基本信息、图片、销售属性值管理、价格查询。
- 商品评价：商品评价（SpuComment）与评价回复（CommentReplay）。
- **商品上架**：将 SPU 下全部 SKU 拼装为 `SkuEsModel` 并通过 Feign 写入搜索中心 ES 索引。

**核心实体**：Category、Brand、Attr、AttrGroup、SpuInfo、SpuImages、SkuInfo、SkuImages、SkuSaleAttrValue、SpuComment。

### 5.2 搜索中心（mall-search）

**职责**：基于 Elasticsearch 的商品全文检索与筛选。

**功能列表**：

- 商品上架写入：接收商品中心推送的 SKU 列表，批量写入 ES（`/search/save/product`）。
- 商品检索页：按关键词、分类、品牌、属性、价格区间、销量、库存等进行多维度检索与过滤、排序、分页、聚合（分类/品牌/属性面包屑），渲染搜索结果页 `/list.html`。

**索引核心字段**：skuId、spuId、skuTitle（IK 中文分词）、skuPrice、skuImg、saleCount、hotScore、brandId/brandName、catelogId/catelogName、attrs（nested 规格属性）。

### 5.3 购物车（mall-cart）

**职责**：用户购物车的增删改查，数据存于 Redis。

**功能列表**：

- 添加商品到购物车（支持登录态与未登录态：通过 `user-key` Cookie 标识临时用户，登录后合并）。
- 购物车列表展示 `/cart.html`。
- 切换商品选中状态 `/checkItem`。
- 修改商品数量 `/countItem`。
- 删除购物车商品 `/deleteItem`。
- 获取当前用户选中结算项 `/currentUserCartItems`（供下单使用）。

### 5.4 认证中心（mall-auth-server）

**职责**：前台用户注册、登录、社交登录与单点登录。

**功能列表**：

- 短信验证码：发送注册验证码，Redis 缓存并设置 60 秒防刷、10 分钟有效期。
- 账号注册：校验验证码、远程调用会员中心注册、校验失败回显错误。
- 账号密码登录：远程调用会员中心登录，登录信息写入 Session。
- 社交登录（微博 OAuth2.0）：授权回调换取 access\_token，自动注册/登录社交用户并写入 Session。
- 微信扫码登录：`WxApiController`（代码中已注释，预留微信开放平台扫码登录能力）。

### 5.5 营销中心（mall-coupon）

**职责**：优惠券、满减、折扣、秒杀等营销能力。

**功能列表**：

- 优惠券管理：优惠券 CRUD、领取记录（CouponHistory）、会员价（MemberPrice）。
- SPU 积分（SpuBounds）与 SKU 满减（SkuFullReduction）、SKU 阶梯价（SkuLadder）。
- 优惠券与 SPU 关联（CouponSpuRelation、CouponSpuCategoryRelation）。
- 秒杀：秒杀活动（SeckillPromotion）、秒杀场次（SeckillSession）、秒杀商品关联（SeckillSkuRelation）、秒杀到货提醒（SeckillSkuNotice）。
- 首页运营：首页广告（HomeAdv）、专题活动（HomeSubject）、专题与 SPU 关联（HomeSubjectSpu）。

### 5.6 会员中心（mall-member）

**职责**：会员账户、等级、积分、地址、收藏等。

**功能列表**：

- 会员管理：会员 CRUD、注册、登录、社交登录（OAuth/微信）接口（供认证中心远程调用）。
- 会员等级（MemberLevel）与等级权益。
- 成长值变动历史（GrowthChangeHistory）、积分变动历史（IntegrationChangeHistory）。
- 会员收货地址（MemberReceiveAddress）。
- 收藏：收藏 SPU（MemberCollectSpu）、收藏专题（MemberCollectSubject）。
- 会员登录日志（MemberLoginLog）、会员统计信息（MemberStatisticsInfo）。

### 5.7 库存中心（mall-ware）

**职责**：仓库、库存与采购履约。

**功能列表**：

- 仓库管理（WareInfo）：仓库 CRUD。
- 库存 SKU 管理（WareSku）：商品在各仓库的库存。
- 采购需求与采购单（Purchase、PurchaseDetail）：采购单合并、分配、领取、完成。
- 库存工作单（WareOrderTask、WareOrderTaskDetail）：订单库存扣减履约任务及明细。

### 5.8 订单中心（mall-order）

**职责**：订单全生命周期与售后。

**功能列表**：

- 订单主单（Order）与订单项/子单（OrderItem）管理。
- 订单操作历史（OrderOperateHistory）：状态流转审计。
- 订单配置（OrderSetting）：自动确认收货、超时关单等时效参数。
- 支付流水（PaymentInfo）与退款流水（RefundInfo）。
- 售后：退货申请（OrderReturnApply）、退货原因字典（OrderReturnReason）。

### 5.9 第三方服务（mall-third-party）

**职责**：统一封装外部云服务能力。

**功能列表**：

- 阿里云 OSS：服务端签名直传（生成上传 Policy/Signature，前端直传对象存储）。
- 短信服务：发送短信验证码（供认证中心调用）。

### 5.10 网关（mall-gateway）

**职责**：统一入口与路由。

**功能列表**：

- API 路由转发：按路径前缀（`/api/product/**`、`/api/order/**` 等）与域名（gulimall.com、search、auth、cart、order、member）路由至对应微服务。
- 统一鉴权、限流（配合 Sentinel）入口。

### 5.11 后台管理（renren-fast + renren-fast-vue）

**职责**：运营后台。

**功能列表**：

- 基于 renren-fast（Spring Boot + Shiro + JWT + MyBatis-Plus）的后端服务，库 `mall_admin`（sys\_user、sys\_role、sys\_menu 等）。
- 基于 renren-fast-vue（Vue2 + Element UI）的前台界面，提供商品、订单、营销、会员、库存等模块的可视化管理。

### 5.12 公共基础（mall-common）

**职责**：统一基础设施。

- 统一响应对象 `R`、异常枚举 `BizCodeEnum`、通用 TO/VO（如 `SkuEsModel`、`MemberResponseVo`）、工具类、常量。

### 5.13 SSO 演示模块（mall-sso-server-test / mall-sso-client-test）

**职责**：单点登录示例。

- `mall-sso-server-test`：SSO 服务端登录（`/login`），签发会话/令牌。
- `mall-sso-client-test`：SSO 客户端受保护资源示例（HelloController），演示一处登录、多端通行。

***

## 6. 核心业务流程

### 6.1 商品检索流程

```
用户 → Nginx → 网关 → mall-search → Elasticsearch → 渲染结果页
```

支持关键词（IK 中文分词）+ 分类/品牌/属性/价格/销量多维过滤排序。

### 6.2 商品上架流程

```
运营上架 → mall-product 拼装 SkuEsModel → Feign → mall-search → 写入 ES
```

上架后商品方可被检索。

### 6.3 加入购物车流程

```
用户加购 → mall-cart
  - 未登录：用 user-key Cookie 存入 Redis 临时购物车
  - 已登录：存入用户购物车；登录后合并临时购物车
```

### 6.4 下单流程

```
用户下单 → 网关 → mall-order
  ├→ mall-product（价格/库存校验）
  ├→ mall-ware（库存扣减，Redis 分布式锁保证一致性）
  ├→ mall-coupon（优惠券/满减核销）
  └→ 创建订单与订单项，生成支付流水
```

涉及 Feign 远程调用与分布式事务考量。

### 6.5 注册/登录流程

```
注册：发送验证码 → Redis 缓存 → 校验 → 远程会员中心注册
登录：账号密码/社交授权 → 会员中心校验 → 写入 Session → 回跳前台
```

***

## 7. 非功能性需求

### 7.1 技术栈（约束）

| 领域    | 选型                                                | 版本                         |
| :---- | :------------------------------------------------ | :------------------------- |
| 语言    | Java                                              | 1.8                        |
| 框架    | Spring Boot / Spring Cloud / Spring Cloud Alibaba | 2.2.5 / Hoxton.SR3 / 2.2.1 |
| ORM   | MyBatis-Plus                                      | 3.4.2                      |
| 数据库   | MySQL                                             | 8.0.23                     |
| 搜索    | Elasticsearch                                     | 7.4.2                      |
| 缓存    | Redis                                             | -                          |
| 注册/配置 | Nacos                                             | 1.2.1                      |
| 网关    | Spring Cloud Gateway                              | -                          |
| 熔断限流  | Sentinel                                          | -                          |
| 远程调用  | OpenFeign                                         | -                          |
| 前端    | Vue2 / Element UI                                 | 2.5.16 / 2.8.2             |
| 模板引擎  | Thymeleaf                                         | -                          |

### 7.2 性能需求

- 商品检索基于 ES，支持中文分词与高并发查询。
- 购物车、Session 等热点数据使用 Redis，降低数据库压力。
- 网关层结合 Sentinel 进行限流与熔断降级，保障稳定性。

### 7.3 安全需求

- 后台管理：Shiro + JWT 认证。
- 前台用户：Spring Security + Redis Session（含子域 Session 共享设计）。
- 社交登录：OAuth2.0（微博/微信）。
- 密码采用 BCrypt 加密；敏感配置托管 Nacos；接口入参 JSR-303 校验；认证接口具备验证码防刷。

### 7.4 可用性 / 可扩展性

- 微服务按业务垂直拆分，独立部署、独立数据库（多库：mall\_pms/oms/wms/sms/ums/admin）。
- 服务注册发现、配置中心、链路追踪（Zipkin）支撑运维可观测性。

***

## 8. 数据需求

| 数据库         | 所属模块 | 核心表                                                                               |
| :---------- | :--- | :-------------------------------------------------------------------------------- |
| mall\_pms   | 商品   | pms\_category、pms\_brand、pms\_spu\_info、pms\_sku\_info、pms\_attr、pms\_attr\_group |
| mall\_oms   | 订单   | oms\_order、oms\_order\_item、oms\_payment\_info、oms\_order\_return\_apply          |
| mall\_wms   | 库存   | wms\_ware\_info、wms\_ware\_sku、wms\_purchase                                      |
| mall\_sms   | 营销   | sms\_coupon、sms\_spu\_bounds、sms\_sku\_full\_reduction、sms\_seckill\_\*           |
| mall\_ums   | 会员   | ums\_member、ums\_member\_level、ums\_member\_receive\_address                      |
| mall\_admin | 后台   | sys\_user、sys\_role、sys\_menu                                                     |

***

## 9. 接口与集成需求

- 统一响应格式：`R{code, msg, data}`（code=0 成功）。
- 服务间调用：OpenFeign（商品→优惠券/搜索/库存；购物车→商品；认证→会员/第三方 等），需配置 Sentinel 熔断降级（fallback）。
- 网关路由：域名 + 路径双模式路由，见网关配置。
- 第三方：阿里云 OSS 签名直传、短信服务接口。

***

## 10. 部署与运行环境需求

- 中间件：Nginx（反向代理/负载均衡）、Nacos（8848）、Redis、MySQL（5.7+）、Elasticsearch（7.4.2）、Kibana（7.4.2）。
- 域名规划（示例 IP 192.168.163.131）：gulimall.com、search.gulimall.com、item.gulimall.com、auth.gulimall.com、cart.gulimall.com。

***

## 11. 验收标准（建议）

1. 商品可完成分类/品牌/属性/SPU/SKU 全链路管理与上架。
2. 前台可按关键词与多维条件检索商品并渲染结果。
3. 未登录/登录用户均可操作购物车，登录后自动合并。
4. 用户可注册、账号登录、社交登录（微博），Session 正确维持。
5. 下单可联动校验价格/库存/优惠券并生成订单与支付流水。
6. 后台可管理订单、营销、会员、库存并配置权限。
7. 网关路由、Sentinel 限流、Nacos 配置生效。

***

## 12. 待确认 / 已知缺口

- 微信扫码登录（WxApiController）当前为注释状态，需补全配置后启用。
- 支付环节（微信/支付宝回调）在代码中以支付流水表预留，具体支付网关集成待补充。
- 秒杀（Seckill）完整链路（与 mall-coupon 的 Seckill 表、缓存预热、防超卖）需进一步落地验证。
- 分布式事务在跨服务下单场景中采用本地消息/最终一致性方案，需明确选型（Seata 等）。

***

*本文档依据项目实际源码梳理，功能描述以代码实现为准；尚未实现的预留能力已在第 12 节标注。*
