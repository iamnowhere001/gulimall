spring:
  application:
    name: gulimall-auth-server

  cloud:
    sentinel:
      filter:
        enabled: false
    nacos:
      config:
        server-addr: localhost:8848
        namespace: e0dfe800-b4e8-4d45-a811-9cfb99018618
